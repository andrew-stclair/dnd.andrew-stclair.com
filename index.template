<html>

<head>
    <title>D&D - Index of {{ page.url }}</title>
    <style>
        ul {
            list-style: none;
            margin-left: 0;
            padding-left: 0;
        }

        .file:before {
            content: "📄";
            padding-right: 5px;
        }

        .directory:before {
            content: "📂";
            padding-right: 5px;
        }

        .back:before {
            content: "↩️";
            padding-right: 5px;
        }
    </style>
</head>

<body>
    <h1>Index of {{ page.url }}</h1>
    <hl />
    <ul>
        <li class="back"><a href="..">Up one level</a></li>
        {%- for item in pagedata[0].contents %}<li class="{{ item.type }}"><a href="{{ item.name }}">{{ item.name }}</a></li>{%- endfor %}
    </ul>
</body>

</html>